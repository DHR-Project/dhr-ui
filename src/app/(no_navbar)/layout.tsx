import { PropsWithChildren } from "react";

export const metadata = {
  title: "No_NBGroupLayout",
  description: "No_NBGroupLayout",
};

export default function No_NBGroupLayout(props: PropsWithChildren) {
  return <>{props.children}</>;
}
